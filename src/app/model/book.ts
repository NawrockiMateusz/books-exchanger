export interface Book {
  id: string;
  isbn: string;
  title: string;
  author: string;
  genre: string;
  city: string;
  // shipping: boolean;
  // destructionLevel: number;
  // image: string;
  userEmail: string;
}
